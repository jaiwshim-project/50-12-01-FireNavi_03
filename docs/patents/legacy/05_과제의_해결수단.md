# 과제의 해결수단

## 개요

본 발명은 위의 6가지 기술적 과제를 해결하기 위해 **통합 의사결정 엔진(Integrated Decision Engine)**을 핵심으로 하는 시스템을 제공한다. 이 엔진은 6개의 서브 모듈로 구성되며, 각각 다음과 같이 작동한다:

---

## 1. 다층적 최적 위치 결정 알고리즘

### 1.1 Position Score 계산 수식

각 후보 위치 P에 대해 다음의 통합 점수를 계산한다:

```
Position Score = Upwind(P) + ExitAccess(P) - PassengerBlock(P) 
                 + MultiFireCover(P) - FFOverlap(P) + CorridorBonus(P)
```

### 1.2 각 요소별 상세 계산

#### 1.2.1 상풍 접근 점수 (Upwind Score) — 최대 40점

```
Upwind(P) = {
    40점        : P가 현재 풍향의 상풍 방향에 있으면
    30점        : P가 상풍 방향 ±30도 범위 내
    20점        : P가 상풍 방향 ±60도 범위 내
    10점        : P가 상풍 방향 ±90도 범위 내
    0점         : 그 외 방향
}

계산 로직:
- 현재 풍향 벡터 = (windX, windY)
- 화재 F에서 위치 P로의 벡터 = (Px - Fx, Py - Fy)
- 상풍 방향 = 풍향 벡터의 반대 방향

θ = acos((위치벡터 · 상풍벡터) / (|위치벡터| × |상풍벡터|))

if θ < 30도:   Upwind(P) = 40
else if θ < 60도: Upwind(P) = 30
else if θ < 90도: Upwind(P) = 20
else if θ < 120도: Upwind(P) = 10
else:          Upwind(P) = 0
```

**적용 사례:**
- 남풍(↑)이 불 때, 화재 남쪽 위치는 40점
- 화재 남동쪽 위치는 30점 (±30도 범위)
- 화재 남서쪽 위치는 10~20점

#### 1.2.2 출구 접근성 점수 (Exit Access Score) — 최대 30점

```
ExitAccess(P) = {
    30점 - (거리/최대거리 × 30) : P에서 가장 가까운 출구까지 거리
}

계산 로직:
- 각 출구 E에 대해 유클리드 거리 계산: dist_E = √((Px - Ex)² + (Py - Ey)²)
- 최소 거리: min_dist = min(dist_E1, dist_E2, ..., dist_En)
- 건물 대각선 길이: max_dist = 최대 가능 거리 (예: 300 미터)

ExitAccess(P) = max(0, 30 - (min_dist / max_dist) × 30)

예시:
- min_dist = 0 미터 (출구에 인접) → ExitAccess = 30점
- min_dist = 150 미터 (절반) → ExitAccess = 15점
- min_dist = 300 미터 (최원거리) → ExitAccess = 0점
```

**적용 사례:**
- 출구 바로 옆: 30점
- 100m 거리: 20점
- 200m 거리: 10점
- 300m 이상: 0점

#### 1.2.3 승객 대피 경로 비간섭 점수 (Passenger Block Score) — 최대 5N점 감점

```
PassengerBlock(P) = Σ(5점 × N_interfere)

계산 로직:
1. 각 승객 Agent의 목표 경로 (Path from Agent to Exit) 계산
2. 위치 P가 각 승객 경로와 교차하는지 판정
   - P 근처 일정 반경(예: 3m) 내에 경로가 있으면 교차
3. 교차하는 승객 수 카운트: N_interfere

PassengerBlock(P) = 5 × N_interfere (최대 음수값, 감점)

예시:
- 5명 승객 경로 차단 → -25점 감점
- 10명 승객 경로 차단 → -50점 감점
- 0명 차단 → 0점
```

**적용 사례:**
- 복도 중앙: 많은 승객 경로 차단 → 큰 감점
- 구석 위치: 경로 교차 최소 → 감점 적음

#### 1.2.4 복수 화재 동시 커버 점수 (Multi-Fire Cover Bonus) — 최대 10점

```
MultiFireCover(P) = {
    10점  : P가 2개 이상의 화재를 동시에 커버할 수 있으면
    0점   : 그 외
}

계산 로직:
1. 각 화재까지의 거리 계산: dist_Fi = √((Px - Fix)² + (Py - Fiy)²)
2. 커버 가능 거리 설정 (예: 50미터)
3. 커버 가능 화재 수: N_cover = Count(dist_Fi < 50)

MultiFireCover(P) = {
    10점  : N_cover >= 2
    0점   : N_cover < 2
}

예시:
- P가 화재 A(30m), 화재 B(40m) 모두 커버 → 10점 보너스
- P가 화재 A만 커버(B는 60m) → 0점
```

**적용 사례:**
- 두 화재 사이 중앙에 위치 → 보너스
- 한 화재만 근처 → 보너스 없음

#### 1.2.5 기존 소방관 중복 배치 방지 (Firefighter Overlap Penalty) — 최대 20점 감점

```
FFOverlap(P) = -20 × N_near

계산 로직:
1. 기존에 배치된 소방관 목록: FF1, FF2, ..., FFn
2. 각 소방관까지의 거리 계산: dist_FFi = √((Px - FFix)² + (Py - FFiy)²)
3. 최소 거리 설정 (예: 40미터) → 이 범위 내 소방관 카운트

N_near = Count(dist_FFi < 40)

FFOverlap(P) = -20 × N_near (단, 최소 -60점 = 3명 이상 근처)

예시:
- P 근처에 기존 소방관 1명 → -20점
- P 근처에 기존 소방관 2명 → -40점
- P 근처에 기존 소방관 3명 이상 → -60점 (상한)
```

**적용 사례:**
- 이미 소방관이 배치된 지역 → 큰 감점
- 다른 소방관 없는 지역 → 감점 없음

#### 1.2.6 복도 위치 우선 (Corridor Bonus) — 최대 15점

```
CorridorBonus(P) = {
    15점  : P가 건물 복도(또는 통로) 위치이면
    0점   : 그 외 (객실, 밀폐된 공간 등)
}

계산 로직:
1. 건물 맵에 "복도" 영역 사전 정의
2. P의 좌표가 복도 폴리곤 내에 있는지 판정

CorridorBonus(P) = 15점 if P ∈ Corridor else 0점
```

**적용 사례:**
- 긴 복도 위치 → 15점 보너스 (이동 안전성 높음)
- 객실 내부 → 0점
- 좁은 통로 → 대기 중인 승객과 충돌 가능성이 낮아 우대

### 1.3 최종 위치 선택

모든 후보 위치에 대해 Position Score를 계산하고, **가장 높은 점수를 가진 위치에 소방관 배치**를 추천한다.

```
Optimal Location = argmax(Position Score) for all candidates P
```

**예시 계산:**

| 위치 | 상풍(40) | 출구(30) | 승객(-5N) | 복수화재(10) | 중복(-20) | 복도(15) | 합계 |
|------|---------|---------|---------|------------|----------|--------|------|
| P1 | 40 | 25 | -15 | 10 | 0 | 15 | **75점** ✓ |
| P2 | 20 | 20 | -25 | 0 | -20 | 0 | **-5점** |
| P3 | 30 | 15 | -5 | 10 | -20 | 15 | **45점** |
| P4 | 10 | 30 | -30 | 0 | 0 | 0 | **10점** |

→ **P1이 선택됨 (75점)**

---

## 2. 화재 우선순위 자동 결정 알고리즘

### 2.1 Threat Score 계산 수식

각 화재 F에 대해 다음의 위협도 점수를 계산한다:

```
Threat Score(F) = PassengerCount(F) × 100 
                  + BlockedPassengers(F) × 30
                  + SmokeAffectedPassengers(F) × 15
                  + ExitThreats(F) × 40
                  + FireRadius(F) × 2
                  - ExistingFirefighters(F) × 80
```

### 2.2 각 요소별 상세 계산

#### 2.2.1 화재 내 승객 수 (PassengerCount × 100점)

```
PassengerCount(F) = |{Agent a : distance(a, F) < FireRadius(F)}|

계산 로직:
1. 화재 F의 반경 범위 내에 있는 모든 승객 Agent 카운트
2. 100점을 곱하여 비중 강조

예시:
- 화재 반경 내 승객 5명 → 500점
- 화재 반경 내 승객 10명 → 1,000점
- 화재 반경 내 승객 0명 → 0점
```

#### 2.2.2 경로 차단 승객 수 (BlockedPassengers × 30점)

```
BlockedPassengers(F) = |{Agent a : 
  ExitPath(a) ∩ SmokeRange(F) ≠ ∅}|

계산 로직:
1. 각 승객 a의 현재 위치에서 목표 출구까지의 경로 계산
2. 화재의 연기 영향 범위(SmokeRange)와 경로의 교차 확인
3. 교차하는 승객의 수를 카운트
4. 30점을 곱하여 우선순위 강조

예시:
- 10명 승객의 대피 경로 차단 → 300점
- 5명 승객의 대피 경로 차단 → 150점
- 경로 차단 없음 → 0점
```

**의의:** 경로 차단은 대피 불가 상황을 의미하므로 즉각 대응 필요

#### 2.2.3 연기 영향 승객 수 (SmokeAffectedPassengers × 15점)

```
SmokeAffectedPassengers(F) = |{Agent a : 
  distance(a, F) < SmokeRadius(F) AND a not yet evacuated}|

계산 로직:
1. 화재 F의 현재 연기 확산 반경 계산
2. 이 범위 내에 있는 미대피 승객 카운트
3. 15점을 곱하여 우선순위 반영

예시:
- 연기 영향 20명 → 300점
- 연기 영향 10명 → 150점
- 연기 영향 없음 → 0점
```

#### 2.2.4 출구 위협 수 (ExitThreats × 40점)

```
ExitThreats(F) = |{Exit e : 
  distance(F, e) < EffectiveRange(F)}|

계산 로직:
1. 각 출구 e에 대해 화재까지의 거리 계산
2. 화재의 영향 범위(열, 연기) 내에 있는 출구 수 카운트
3. 40점을 곱하여 높은 우선순위 부여

예시:
- 1개 출구 위협 → 40점
- 2개 출구 위협 → 80점
- 모든 출구 위협 → 치명적 상황
```

**의의:** 출구 차단은 승객 전원이 대피 불가능한 상황

#### 2.2.5 화재 반경 크기 (FireRadius × 2점)

```
FireRadius(F) = radius(F) in pixels/meters

계산 로직:
- 화재의 시각적 반경을 직접 사용
- 큰 화재일수록 더 많은 위협도 제공

예시:
- 반경 50미터 → 100점
- 반경 100미터 → 200점
- 반경 200미터 → 400점
```

#### 2.2.6 기존 투입 소방관 수 (ExistingFirefighters × -80점)

```
ExistingFirefighters(F) = |{Firefighter ff : 
  distance(ff, F) < 60 meters}|

계산 로직:
1. 화재 근처(60m 이내)에 이미 배치된 소방관 수 카운트
2. 80점을 곱하여 감점 (이미 대응 중이므로 우선순위 낮음)

예시:
- 기존 소방관 1명 배치 → -80점
- 기존 소방관 2명 배치 → -160점
- 기존 소방관 3명 배치 → -240점
```

### 2.3 우선순위 결정

모든 화재에 대해 Threat Score를 계산하고, **점수가 높은 순서대로 우선순위 결정**:

```
Priority Ranking = sort(Fires, by Threat Score descending)

Recommended Action:
1. Priority #1 화재: 소방관 4명 즉시 투입
2. Priority #2 화재: 소방관 2명 투입 또는 대기
3. Priority #3 화재: 모니터링 중, 추가 발생 시 재평가
```

**예시 계산:**

| 화재 | 승객(×100) | 차단(×30) | 연기(×15) | 출구(×40) | 반경(×2) | 소방(×-80) | 합계 | 순위 |
|------|----------|---------|---------|----------|---------|----------|------|------|
| A | 800 | 300 | 150 | 80 | 100 | -160 | **1,270** | #1 ✓ |
| B | 500 | 150 | 100 | 0 | 50 | 0 | **800** | #2 |
| C | 300 | 0 | 50 | 40 | 80 | 0 | **470** | #3 |
| D | 200 | 0 | 30 | 0 | 20 | 0 | **250** | #4 |

→ **화재 A를 최우선으로 소방관 4명 투입**

---

## 3. 이중(雙重) 경로 탐색 시스템

### 3.1 A* 경로 탐색 알고리즘의 이중화

#### 3.1.1 기본 A* 수식

```
f(node) = g(node) + h(node)

여기서:
- g(node) = 시작점에서 현재 노드까지의 실제 이동 비용
- h(node) = 현재 노드에서 목표점까지의 휴리스틱 거리 (직선 거리)
- f(node) = 총 추정 비용 (낮을수록 최우선 탐색)
```

#### 3.1.2 Safety-First A* (승객용)

```
g(node) = 기본 이동 비용 + 화재 회피 비용
        = distance_to_node + fireDangerAt(node) × danger_multiplier

fireDangerAt(node) = {
    0        : node가 화재 영향 범위 밖
    10~50    : node가 연기 영향 범위 내
    100~200  : node가 화재 반경 내
    500+     : node가 화재 중심 근처 (통과 불가)
}

h(node) = euclidean_distance(node, exit)

적용 사례:
┌─────────────────┐
│ 🔥화재          │
│ (fireDangerAt=100)
│                 │
│  [A]────[B]     │
│   노드A 근처     │
│   fireDangerAt=50│
│                 │
└─────────────────┘

A* 탐색 결과:
- 직선 거리는 짧지만 fireDangerAt 높음
- → [A] 경로 회피
- → [B]를 거쳐 우회로 선택
```

#### 3.1.3 Straight-Line A* (소방관용)

```
g(node) = 기본 이동 비용 (화재 회피 비용 제외)
        = distance_to_node

fireDangerAt(node) = 0 (무시, 적용 안함)

h(node) = euclidean_distance(node, fire_target)

적용 사례:
┌─────────────────┐
│ 🔥화재          │
│ (fireDangerAt 무시)
│                 │
│  [A]────[B]     │
│   노드A 근처     │
│                 │
└─────────────────┘

A* 탐색 결과:
- 순수 거리 기반으로 최단 경로 선택
- → [A] 경로 직진 (fire 방향)
- → 화재까지 최단 거리로 진입
```

### 3.2 경로 충돌 분석

```
CollisionRisk = |{intersection point : 
  PassengerPath ∩ FirefighterPath ≠ ∅}|

분석 로직:
1. 각 승객의 Safety-First A* 경로 계산
2. 각 소방관의 Straight-Line A* 경로 계산
3. 두 경로의 교차점 확인
4. 교차점에서의 충돌 가능성 평가

위험 평가:
- 교차점이 없음: 안전함
- 교차점이 1~2개: 조정 가능
- 교차점이 3개 이상: 심각한 충돌 위험
```

---

## 4. 환경 물리량의 실시간 시뮬레이션

### 4.1 풍향 시뮬레이션

```
WindVector(t) = BaseWind + LocalDraft(t)

여기서:
- BaseWind = 외부 일정한 바람 (설정값)
- LocalDraft(t) = 화재로 인한 대류 열기 (시간 변수)

LocalDraft(t) = HeatIntensity(t) × direction_away_from_fire

HeatIntensity(t) = exp(-t / decay_time) (화재 진압에 따라 감소)
```

### 4.2 연기 확산 시뮬레이션

```
SmokeRadius(t, F) = FireRadius(F) + expansion_rate × t × windFactor

여기서:
- FireRadius(F) = 화재 F의 초기 반경
- expansion_rate = 단위 시간당 확장 속도 (예: 2픽셀/프레임)
- t = 경과 시간 (프레임 수)
- windFactor = 풍향에 따른 비대칭 확장

비대칭 확산 로직:
1. 풍향 방향: 빠른 확산 (expansion_rate × 1.5)
2. 풍향 반대: 느린 확산 (expansion_rate × 0.5)
3. 측면: 중간 속도 (expansion_rate × 1.0)
```

### 4.3 가시거리 감소 모델

```
Visibility(node, t) = BaseVisibility × exp(-SmokeConcentration(node, t) / visibility_decay)

SmokeConcentration(node, t) = {
    0        : distance(node, fire) > SmokeRadius(t)
    1.0      : distance(node, fire) < FireRadius(F)
    gradient : 선형 감소 (SmokeRadius 범위 내)
}

가시거리 영향:
- Visibility = 100%: 가시성 완전 (연기 없음)
- Visibility = 50%: 가시성 저하 (근거리만 식별 가능)
- Visibility = 10%: 거의 맹목 상태 (목표 위치 미확인)
```

---

## 5. 시각화 및 의사결정 결과 표시

### 5.1 마커 표시

```
각 추천 위치 P에 대해:
1. 보라색 펄스 십자(+) 마커 렌더링
   - x: P.x, y: P.y
   - 색상: RGB(200, 100, 255) (보라색)
   - 애니메이션: 0.5초 주기로 크기 변동 (10px ~ 20px)

2. 우선순위 라벨 표시
   - 텍스트: "AI #1", "AI #2", "AI #3", "AI #4"
   - 색상: 흰색 (검은색 테두리)
   - 위치: 마커 위쪽 텍스트

3. 점선 연결선 렌더링
   - 시작: 추천 위치 P
   - 종료: 대응 화재 F
   - 스타일: 점선 (3px 점, 3px 간격)
   - 색상: 분홍색 또는 주황색
```

### 5.2 방향 텍스트 표시

```
8방향 나침반 계산:
- 벡터 = (Px - Fx, Py - Fy) (화재에서 추천 위치로의 방향)
- 각도 = atan2(벡터.y, 벡터.x) in degrees

방향 결정:
if -22.5° ≤ 각도 < 22.5°:     "우측"
elif 22.5° ≤ 각도 < 67.5°:    "우하단"
elif 67.5° ≤ 각도 < 112.5°:   "하부"
elif 112.5° ≤ 각도 < 157.5°:  "좌하단"
elif -157.5° ≤ 각도 < -112.5°: "좌상단"
elif -112.5° ≤ 각도 < -67.5°: "상부"
elif -67.5° ≤ 각도 < -22.5°:  "우상단"
elif 157.5° ≤ 각도 ≤ 180° or -180° ≤ 각도 < -157.5°: "좌측"

텍스트 렌더링:
- 위치: 화재에서 추천 위치 방향, 중간지점 근처
- 폰트: 12pt, 회전: 0도 (수평)
- 색상: 흰색 텍스트 (검은색 테두리로 강조)
```

---

## 6. 자동 투입 의사결정 엔진

### 6.1 의사결정 플로우

```
AI 최적 투입 버튼 클릭
    ↓
[Step 1] 모든 화재의 Threat Score 재계산
    ↓
[Step 2] 점수 상위 화재 선택 (Priority #1)
    ↓
[Step 3] 선택 화재에 대해 Position Score 상위 4개 위치 선택
    ↓
[Step 4] 각 위치에 소방관 객체 4명 생성
    ↓
[Step 5] Straight-Line A* 경로 탐색 (각 소방관별)
    ↓
[Step 6] 경로 이동 개시 (초속 2m 등)
    ↓
[Step 7] 화재 진압 또는 새 화재 발생 시 Step 1로 돌아감 (재평가)
```

### 6.2 상세 알고리즘

#### 6.2.1 최우선 화재 선택

```
ThreatScores = {
    Fire_1: score_1,
    Fire_2: score_2,
    ...,
    Fire_n: score_n
}

SelectedFire = argmax(ThreatScores)
```

#### 6.2.2 최적 위치 4곳 선택

```
PositionScores = {
    Pos_1: score_1,
    Pos_2: score_2,
    ...,
    Pos_m: score_m
} (SelectedFire에 대해서만 계산)

TopLocations = sort(PositionScores, descending)[:4]

배치 위치:
- Firefighter #1 → TopLocations[0]
- Firefighter #2 → TopLocations[1]
- Firefighter #3 → TopLocations[2]
- Firefighter #4 → TopLocations[3]
```

#### 6.2.3 경로 탐색 및 이동

```
for each Firefighter ff in TopLocations:
    StartPos = ff의 현재 위치
    TargetPos = SelectedFire의 위치
    Path = Straight-Line A*(StartPos, TargetPos)
    ff.setPath(Path)
    ff.startMovement(speed = 2.0 m/s)
```

---

## 종합 시스템 동작 흐름

```
시뮬레이션 시작 (매 프레임마다)
    ↓
[1] 환경 업데이트
    ├─ 풍향 시뮬레이션 (BaseWind + LocalDraft 계산)
    ├─ 연기 확산 범위 업데이트
    ├─ 승객 위치 업데이트 (Safety-First A* 경로 따라 이동)
    ├─ 소방관 위치 업데이트 (Straight-Line A* 경로 따라 이동)
    └─ 화재 상태 업데이트 (반경, 진압 여부 등)
    ↓
[2] 의사결정 엔진 실행
    ├─ Position Score 재계산 (모든 후보 위치)
    ├─ Threat Score 재계산 (모든 화재)
    └─ 현재 추천 위치 및 우선순위 화재 결정
    ↓
[3] 시각화 업데이트
    ├─ 보라색 펄스 마커 렌더링 (추천 위치)
    ├─ 우선순위 라벨 표시 ("AI #1", "AI #2", ...)
    ├─ 점선 연결선 렌더링
    └─ 방향 텍스트 표시
    ↓
[4] 사용자 입력 처리
    ├─ "AI 최적 투입" 버튼 클릭 → 자동 배치 실행
    └─ 기타 수동 조작
    ↓
다음 프레임으로 반복
```

---

## 성능 최적화

### 계산량 관리

```
Position Score 계산:
- 후보 위치 수: ~100개
- 각 위치당 6개 요소 계산
- 매 프레임마다 실행 불필요 → 0.5초마다 재계산

Threat Score 계산:
- 화재 수: ~10개
- 각 화재당 6개 요소 계산
- 매 프레임마다 재계산 (가벼운 계산)

경로 탐색:
- 필요할 때만 실행 (새 투입, 방향 변경 시)
- A* 알고리즘 최적화로 <50ms 내 완료
```

---

## 결론

본 발명의 해결수단은 **6가지 기술적 모듈(위치 최적화, 우선순위 결정, 경로 탐색, 물리 시뮬레이션, 시각화, 자동 투입)**을 통합하여 종래의 수동 배치 방식을 극복하고, **AI 기반의 객관적이고 효율적인 소방관 배치 시스템**을 구현한다.

이를 통해:
- **사용자 개입 최소화:** 클릭 한 번으로 완성
- **의사결정 투명성:** 6가지 요소 기반의 명확한 점수 제시
- **재난 대응 효율성 극대화:** 화재 진압 시간 단축, 승객 안전성 향상
