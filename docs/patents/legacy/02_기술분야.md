# 기술분야

## 관련 기술 분야

본 발명은 다음의 기술 분야에 관한 것이다:

### 1. 재난 시뮬레이션 및 대피 관리 기술
본 발명은 **크루즈선, 대형건물, 지하철역, 공항** 등 대형 밀폐공간에서 발생하는 화재에 대한 실시간 대피 시뮬레이션을 제공한다. 종래의 정적(靜的) 대피 분석과 달리, 본 발명은 **동적 화재 확산**, **실시간 연기 확산 패턴**, **승객 이동 경로**, **소방관 배치 전략**을 동시에 모의실험하는 종합 시뮬레이션 환경을 구현한다.

### 2. 인공지능 기반 의사결정 시스템
본 발명의 핵심은 **Decision Engine(의사결정 엔진)**으로, 다층적 입력 매개변수를 실시간으로 분석하여 최적의 소방관 배치 위치와 우선순위를 자동으로 결정한다. 이는 다음의 AI 기법을 활용한다:

- **다중 평가 모델(Multi-Criteria Evaluation Model):** 6가지 주요 평가 요소(바람 방향, 출구 접근성, 승객 대피 경로, 화재 우선순위, 중복 배치 방지, 복도 위치 선호도)를 가중치 기반으로 종합 평가
- **의사결정 알고리즘:** 각 화재별 위협도를 계산(Threat Scoring System)하여 소방관 투입 우선순위를 결정
- **실시간 최적화:** 새로운 화재 발생, 기존 화재 진압, 승객 이동 변화에 따라 재시간(Re-scoring)을 수행

### 3. 경로 탐색 및 네비게이션 알고리즘
본 발명은 **A* 경로 탐색 알고리즘**의 이중(雙重) 구현을 특징으로 한다:

#### 3.1 승객용 경로 탐색 (Safety-First A*)
- 화재 위험도(fireDangerAt)를 경로 비용(G-cost)에 반영
- 화재 근처는 높은 이동 비용으로 설정하여 자동 회피
- 최단 거리와 안전성을 균형있게 고려

#### 3.2 소방관용 경로 탐색 (Straight-Line A*)
- 화재 위험도를 무시하고 순수 거리 기반으로 최단 경로 계산
- 소방관이 화재를 직접 진압하기 위해 최단 경로로 진입 가능
- 일반 승객과의 경로 분리로 충돌 회피

### 4. 환경 물리 시뮬레이션
본 발명은 다음의 물리량을 실시간으로 계산한다:

- **풍향(Wind Direction):** 건물/선박의 통풍 구조, 외부 바람, 화재로 인한 대류 열기를 반영하여 실시간 풍향 계산
- **연기 확산(Smoke Propagation):** 화재 위치로부터 풍향을 따라 시간에 따라 확장하는 연기 영향 범위 계산
- **열 영향(Heat Impact):** 화재 반경 내 열 피해 범위 계산
- **시각성(Visibility):** 연기에 의한 가시거리 감소 반영

### 5. 실시간 시각화 및 사용자 인터페이스
본 발명은 **캔버스 기반 2D 실시간 시각화**를 제공한다:

- **보라색 펄스 십자(+) 마커:** 추천 소방관 배치 위치의 실시간 표시
- **점선 연결선:** 소방관 배치 위치에서 화재까지의 접근 경로 시각화
- **방향 텍스트:** 8방향 나침반(북쪽, 북동쪽, 동쪽, 남동쪽, 남쪽, 남서쪽, 서쪽, 북서쪽)으로 접근 방향 표시
- **우선순위 라벨:** "AI #1", "AI #2", "AI #3", "AI #4" 등으로 다중 소방관 배치의 우선순위 표시

### 6. 대규모 데이터 처리 및 성능 최적화
본 발명은 다음의 요구사항을 만족한다:

- **실시간 처리:** 밀리초 단위의 Decision Engine 재계산
- **다중 객체 관리:** 수백 명의 승객, 수십 개의 화재 지점, 다중 소방관 배치를 동시 처리
- **메모리 효율성:** 점진적 연기 확산 모델로 메모리 사용량 최소화

---

## 선행 기술과의 관계

### 종래 화재 대피 시뮬레이션 도구
- FDS(Fire Dynamics Simulator): 정적 화재 물리 시뮬레이션에 특화, 대피 경로 최적화 부재
- Pathfinder, Legion: 승객 대피 경로 계산에 특화, 소방관 배치 최적화 기능 부재
- 게임 엔진(Unity, Godot): 일반적인 3D 시뮬레이션, 화재/연기/바람 물리 미포함

### 본 발명의 차별성
본 발명은 **AI 기반 소방관 배치 의사결정**에 초점을 맞추어, 종래 기술의 한계를 극복한다:
- **다층적 평가 모델:** 6가지 요소를 가중치 기반으로 통합
- **자동 의사결정:** 사용자 개입 최소화, 최적 위치 자동 추천
- **이중 경로 탐색:** 승객과 소방관의 서로 다른 대피/진입 전략 지원
- **실시간 시각화:** 추천 위치와 근거를 즉각적으로 표시

---

## 기술의 응용 분야

1. **해운산업:** 크루즈선, 여객선, 해양 구조선의 화재 대응 훈련 및 설계 검증
2. **건축산업:** 고층빌딩, 공항, 역사, 쇼핑몰 등 대형 시설의 화재 안전 계획 수립
3. **재난관리:** 소방청, 안전보건공단의 재난 대응 훈련, 시나리오 기획
4. **건설 안전:** 신규 대형 시설 설계 단계에서 화재 대응 가능성 사전 검증
5. **VR/메타버스:** 몰입형 안전 교육 플랫폼 구현

---

## 표준 및 규격

본 발명은 다음의 국내외 표준을 참고하여 설계되었다:

- **ISO 13571:** 화재 환경에서 인간의 열 및 연기 영향 평가
- **NFPA 101:** 생명 안전 코드 (Life Safety Code)
- **IMO SOLAS:** 국제 해상 인명 안전 규칙 (선박 화재 안전)
- **한국 건축법:** 건축물의 화재 안전 기준
- **한국 해양안전법:** 해양사고 대응 및 안전 관리 기준
