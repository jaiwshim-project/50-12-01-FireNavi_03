# 특허 명세서 — PAT-NEW-02

## 발명의 명칭

**동일 동적 위험지도 상에서 대피자의 위험 회피 경로와 소방관의 위험 진입 경로를 상반된 비용 함수로 동시 생성하되, 두 경로군 간 물리적 간섭을 방지하는 방법 및 시스템**

---

## 기술분야

본 발명은 화재 대피 및 소방 전술 지원 기술에 관한 것으로, 보다 구체적으로는 하나의 동적 위험지도를 기반으로 대피자에게는 위험을 회피하는 경로를, 소방관에게는 위험에 진입하는 경로를 각각 상반된 비용 함수로 동시에 생성하고, 두 경로군이 물리적으로 간섭하지 않도록 시공간 예약 테이블로 조율하는 방법에 관한 것이다.

---

## 배경기술

종래의 화재 대피 시스템은 대피자의 경로 생성만을 목적으로 하며, 소방관의 진입 경로는 별도의 시스템이나 소방관의 경험적 판단에 의존한다.

그러나 실제 화재 현장에서는 대피자와 소방관이 동일한 복도와 계단을 사용하며, 이 두 그룹의 이동 방향은 정반대이다. 대피자는 화재로부터 멀어지고, 소방관은 화재를 향해 진입한다. 이로 인해 다음 문제가 발생한다:

1. 좁은 복도에서 대피자와 소방관이 마주치면 양쪽 모두의 이동이 지연된다.
2. 소방관이 대피자 흐름에 역행하면 대피자에게 공포감을 유발하고 밀집도를 증가시킨다.
3. 두 시스템이 독립적이므로 상호 간섭을 사전에 예방할 수 없다.

기존 연구에서는 대피자 경로 최적화 또는 소방관 경로 최적화 중 하나만을 다루며, 동일 위험지도 상에서 상반된 목적 함수를 갖는 두 경로군을 동시에 생성하고 간섭을 방지하는 통합 시스템은 제시되지 않았다.

---

## 해결하려는 과제

본 발명은 동일한 동적 위험지도를 공유하면서도 정반대의 목적 함수를 갖는 대피자 경로와 소방관 경로를 동시에 생성하고, 시공간 예약 테이블(Space-Time Reservation Table)을 통해 두 경로군 간 물리적 간섭을 방지하는 방법을 제공하는 것을 목적으로 한다.

---

## 과제의 해결수단

### 1. 이원 비용 함수 설계

#### 1.1 대피자용 비용 함수 (Safety-First)

대피자의 경로 탐색 시 적용되는 비용 함수는 위험을 **최대한 회피**하는 방향으로 설계된다:

```
f_evac(n) = g(n) + h(n) + R_evac(n)
```

여기서 R_evac(n)은 위험 회피 페널티로:
```
R_evac(n) = γ_evac × R(n) × (1 + α_type)
```
- `R(n)`: 동적 위험지도에서 노드 n의 위험도 (0.0~1.0)
- `γ_evac`: 위험도 증폭 계수 (기본값 10.0)
- `α_type`: 대피자 유형별 안전 마진 (일반=0.0, 취약계층=1.0)

이 함수에 의해 대피자의 경로는 **위험도가 높은 구역을 우회**한다.

#### 1.2 소방관용 비용 함수 (Straight-Line)

소방관의 경로 탐색 시 적용되는 비용 함수는 위험을 **무시하고 목표(화원)에 최단 접근**하는 방향으로 설계된다:

```
f_fire(n) = g(n) + h_fire(n) + I(n)
```

여기서:
- `h_fire(n)`: 노드 n에서 목표 화원까지의 직선 거리
- `I(n)`: 간섭 페널티 (대피자 경로와의 충돌 비용)

**핵심 차이점:** 위험지도 페널티 R(n)이 **적용되지 않는다.** 대신 간섭 페널티 I(n)이 추가된다.

#### 1.3 간섭 페널티 I(n) 산출

```
I(n) = λ × Σ overlap(n, path_evac_i)
```

여기서:
- `λ`: 간섭 페널티 가중치 (기본값 50.0)
- `overlap(n, path_evac_i)`: 노드 n이 대피자 i의 경로에 포함되고, 시간적으로 ±5초 이내에 동시 통과가 예상되는 경우 1, 아닌 경우 0

이 함수에 의해 소방관은 화원에 최단 접근하되, **대피자 경로와 겹치는 구간을 자동으로 회피**한다.

### 2. 시공간 예약 테이블 (STRT)

두 경로군 간 물리적 간섭을 방지하기 위해 시공간 예약 테이블을 사용한다.

#### 2.1 테이블 구조

```
STRT[node_id][time_slot] = {
    reserved_by: "evacuee" | "firefighter" | null,
    agent_ids: [list],
    direction: vector,     // 이동 방향
    capacity: integer      // 해당 노드의 동시 통과 가능 인원
}
```

- `time_slot`: 1초 단위로 이산화된 시간 구간
- `capacity`: 복도 폭에 따라 결정 (1m 폭당 1.5명/초)

#### 2.2 예약 프로토콜

```
단계 R1: 대피자 경로 우선 예약
    대피자의 경로가 먼저 산출되고, 각 노드-시간 조합을
    STRT에 "evacuee"로 예약한다.
    대피자의 이동 방향을 direction에 기록한다.

단계 R2: 소방관 경로 생성 시 STRT 참조
    소방관의 A* 탐색에서 각 후보 노드를 평가할 때:

    if STRT[n][t_estimated].reserved_by == "evacuee":
        # 대피자 예약 구간
        if direction_conflict(소방관_방향, evacuee_방향):
            # 역방향 충돌 → 간섭 페널티 적용
            I(n) += λ × count(STRT[n][t_estimated].agent_ids)
        else:
            # 동일 방향 → 페널티 감소
            I(n) += λ × 0.2 × count(STRT[n][t_estimated].agent_ids)

    if remaining_capacity(n, t_estimated) <= 0:
        # 용량 초과 → 해당 노드-시간 조합 탐색 불가
        f_fire(n) = INFINITY

단계 R3: 소방관 경로 확정 후 STRT에 "firefighter"로 추가 예약

단계 R4: 경로 갱신 시 STRT 전체 재구축
    매 사이클(100ms)마다 STRT를 초기화하고
    R1~R3을 재실행한다.
```

#### 2.3 역방향 충돌 판정

```
direction_conflict(v_ff, v_evac):
    cos_theta = dot(v_ff, v_evac) / (|v_ff| × |v_evac|)
    if cos_theta < -0.5:   # 120° ~ 240° 범위
        return True        # 역방향 충돌
    else:
        return False       # 동일/직교 방향 → 허용
```

### 3. 소방관 최적 배치 위치 산출

소방관의 목표(화원)에 도달한 후의 배치 위치를 최적화한다.

#### 3.1 Position Score 산출

```
PositionScore(P) = Upwind(P) + ExitAccess(P)
                   - PassengerBlock(P)
                   + MultiFireCover(P)
                   - FFOverlap(P)
                   + CorridorBonus(P)
```

**각 항의 산출 방법:**

**(a) Upwind(P): 풍상 접근 점수 (최대 40점)**
```
Prediction AI로부터 노드 P에서의 기류 방향 벡터 W를 수신한다.
소방관의 접근 방향 벡터 A와 W의 각도 θ를 산출한다.
θ = arccos(dot(A, W) / (|A| × |W|))

Upwind(P) = 40,  if θ ≤ 30°  (풍상 정면)
           = 25,  if 30° < θ ≤ 60°
           = 10,  if 60° < θ ≤ 90°
           = 0,   if θ > 90°  (풍하 방향)
```
근거: 풍상에서 접근하면 연기가 소방관 반대쪽으로 이동하여 가시거리와 호흡 안전성이 확보된다.

**(b) ExitAccess(P): 출구 접근성 점수 (최대 30점)**
```
P에서 가장 가까운 출구까지의 거리 d_exit를 산출한다.
ExitAccess(P) = 30 × max(0, 1 - d_exit / D_max)
여기서 D_max = 100m (최대 허용 거리)
```
근거: 소방관의 긴급 철수(emergency retreat) 경로를 보장한다.

**(c) PassengerBlock(P): 대피자 간섭 페널티 (인당 -5점)**
```
STRT를 참조하여 P 주변 10m 이내에서
대피자 경로와 교차하는 인원 수 N_cross를 산출한다.
PassengerBlock(P) = -5 × N_cross
```

**(d) MultiFireCover(P): 다중 화원 커버 점수 (최대 10점)**
```
P에서 반경 50m 이내에 존재하는 화원 수 N_fires를 산출한다.
MultiFireCover(P) = min(10, 5 × N_fires)
```

**(e) FFOverlap(P): 소방관 중복 페널티 (-20점)**
```
P에서 반경 40m 이내에 이미 배치된 소방관이 있으면:
FFOverlap(P) = -20
없으면:
FFOverlap(P) = 0
```
근거: 소방관의 분산 배치로 커버리지를 극대화한다.

**(f) CorridorBonus(P): 복도 보너스 (최대 15점)**
```
P가 위치한 공간의 유형을 BIM 데이터에서 판별한다.
개방 복도: CorridorBonus = 15
일반 실내: CorridorBonus = 5
밀폐 공간: CorridorBonus = 0
```

#### 3.2 Threat Score — 화원별 투입 우선순위

다중 화원 발생 시 어떤 화원에 소방관을 먼저 투입할지 결정한다:

```
ThreatScore(F) = PassengerCount(F) × 100
                + BlockedPassengers(F) × 30
                + SmokeAffectedPassengers(F) × 15
                + ExitThreats(F) × 40
                + FireRadius(F) × 2
                - ExistingFirefighters(F) × 80
```

**각 항의 의미와 계수 근거:**

| 항 | 계수 | 근거 |
|----|------|------|
| PassengerCount × 100 | 화원 반경 내 승객 수에 최대 가중치 | 인명 우선 원칙 |
| BlockedPassengers × 30 | 화재로 경로가 차단된 승객 | 고립 위험 반영 |
| SmokeAffectedPassengers × 15 | 연기 영향권 승객 | 간접 위험 반영 |
| ExitThreats × 40 | 위협받는 출구 수 | 대피 경로 확보 |
| FireRadius × 2 | 화원 크기 (m) | 확산 속도 반영 |
| ExistingFirefighters × -80 | 이미 투입된 소방관 수 | 분산 투입 유도 |

---

## 청구범위

**[청구항 1]**
동적 위험지도를 공유하는 화재 대피 시스템에서,
(a) N명의 대피자 각각에 대해, 위험도가 높은 노드에 양(+)의 페널티를 부과하는 제1 비용 함수로 위험 회피 경로를 산출하고, 산출된 경로의 노드-시간 조합을 시공간 예약 테이블에 예약하는 단계;
(b) M명의 소방관 각각에 대해, 위험도 페널티를 배제하고 목표 화원까지의 거리를 최소화하되, 상기 시공간 예약 테이블을 참조하여 대피자 경로와 역방향 충돌이 발생하는 노드-시간 조합에 간섭 페널티를 부과하는 제2 비용 함수로 진입 경로를 산출하는 단계;
(c) 상기 대피자 경로와 소방관 경로를 매 사이클마다 동시에 갱신하되, 대피자 경로를 우선 예약한 후 소방관 경로를 산출하는 것을 특징으로 하는 대피자-소방관 이원 경로 동시 생성 방법.

**[청구항 2]**
제1항에 있어서,
상기 역방향 충돌 판정은, 소방관의 이동 방향 벡터와 대피자의 이동 방향 벡터의 내적의 코사인 값이 -0.5 미만인 경우(120°~240° 범위)를 역방향 충돌로 판정하는 것을 특징으로 하는 방법.

**[청구항 3]**
제1항에 있어서,
상기 소방관의 목표 화원 도달 후 배치 위치를 결정하기 위해,
풍상 접근 점수, 출구 접근성 점수, 대피자 간섭 페널티, 다중 화원 커버 점수, 소방관 중복 페널티, 복도 보너스를 합산한 위치 점수를 산출하는 단계를 더 포함하는 것을 특징으로 하는 방법.

**[청구항 4]**
제1항에 있어서,
다중 화원 발생 시, 각 화원의 반경 내 승객 수, 경로 차단 승객 수, 연기 영향 승객 수, 위협 출구 수, 화원 크기, 기 투입 소방관 수를 가중합한 위협 점수를 산출하여 소방관 투입 우선순위를 결정하는 단계를 더 포함하는 것을 특징으로 하는 방법.

**[청구항 5]**
제1항 내지 제4항 중 어느 한 항의 방법을 실행하기 위한 프로그램이 기록된 컴퓨터 판독 가능 기록매체.

---

## 발명의 효과

1. 대피자와 소방관이 동일 공간을 공유하는 상황에서 물리적 간섭을 자동으로 방지하여 양쪽의 이동 효율을 동시에 극대화한다.
2. 시공간 예약 테이블에 의해 역방향 충돌이 사전에 방지되어, 좁은 복도에서의 교착 상태(deadlock)가 발생하지 않는다.
3. PositionScore에 의해 소방관이 풍상 방향, 출구 접근 가능, 대피자 비간섭 위치에 자동으로 배치되어 구조 효율과 소방관 안전이 동시에 확보된다.
4. ThreatScore에 의해 제한된 소방 인력이 인명 피해 최소화에 가장 효과적인 화원에 우선 투입된다.

---

## 도면의 간단한 설명

- [도 1] 이원 비용 함수 개념도 (대피자 vs 소방관)
- [도 2] 시공간 예약 테이블 구조 및 예약 프로토콜
- [도 3] 역방향 충돌 판정 기하학적 다이어그램
- [도 4] PositionScore 각 항의 산출 예시
- [도 5] ThreatScore 기반 소방관 투입 우선순위 결정 흐름도
- [도 6] 대피자-소방관 경로 간섭 방지 시나리오 예시

---

*발명자: 심재우 | 출원인: FireNavi*
