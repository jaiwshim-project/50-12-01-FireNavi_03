# Behavior AI Engine — 사람을 이해하는 AI

> 군중 행동 분석 및 취약 계층 식별 엔진  
> FireNavi 자체 개발 | 엔진 컬러: #F59E0B (Orange)

---

## 1. 엔진 개요

Behavior AI는 수천 명의 사람이 동시에 이동할 때 발생하는 군중 역학을 **유체역학적 관점**에서 실시간으로 분석하는 엔진입니다. 대규모 화재 대피에서 두 번째로 위험한 것은 군중 밀집(stampede)이며, 수천 명이 좁은 통로로 동시에 몰리면 압사 사고가 발생합니다. Behavior AI는 병목과 충돌을 사전에 탐지하고, 6가지 유형의 대피자를 구분하여 개인별 이동 특성을 Decision AI에 전달합니다.

### 핵심 역할
- 실시간 군중 밀집도 분석 및 열지도 생성
- 보행자 간 상호작용(반발·회피·따라가기) 시뮬레이션
- 병목 구간 사전 탐지 (임계값: 구역당 90명)
- 6가지 대피자 유형 분류 및 이동 속도 차별화
- 패닉 행동 감지 및 군중 흐름 이상 탐지

---

## 2. 알고리즘 구조

### 2.1 Social Force Model (Helbing 모델 기반 자체 확장)

각 보행자에게 작용하는 힘(Force)을 벡터 합산하여 이동 방향과 속도를 결정합니다.

**총 합력 공식:**
```
F_total = F_desired + F_social + F_wall + F_fire
```

**각 힘의 정의:**

#### F_desired (목표 방향력)
```
F_desired = m × (v₀ × e₀ - v) / τ
```
- `m`: 보행자 질량 (기본 80kg)
- `v₀`: 희망 보행 속도 (유형별 차등)
- `e₀`: 목표 방향 단위 벡터
- `v`: 현재 속도 벡터
- `τ`: 이완 시간 (0.5초)

#### F_social (보행자 간 상호작용)
```
F_social = Σ [A × exp((r_ij - d_ij) / B) × n_ij + k × g(r_ij - d_ij) × n_ij]
```
- `A`: 상호작용 강도 (2000N)
- `B`: 상호작용 범위 (0.08m)
- `r_ij`: 두 보행자의 반경 합
- `d_ij`: 두 보행자 간 거리
- `n_ij`: 보행자 i에서 j를 향하는 단위 벡터
- `k`: 물리적 접촉 시 강성 계수 (120000 kg/s²)
- `g()`: 접촉 함수 (d < r 일 때만 활성)

#### F_wall (벽면 회피력)
```
F_wall = A_w × exp((r_i - d_iw) / B_w) × n_iw
```
- 벽, 기둥, 장애물로부터의 반발력
- 문 통과 시 좁은 개구부에서의 아치 효과(Arch Effect) 재현

#### F_fire (화재 회피력) — FireNavi 독자 개발
```
F_fire = C_fire × exp(-d_fire / R_fire) × n_fire
```
- `C_fire`: 화재 회피 강도 계수
- `d_fire`: 화원까지의 거리
- `R_fire`: 화재 영향 반경
- Prediction AI의 연기 예측 데이터와 실시간 연동

**FireNavi 독자 확장:**
- 원본 Helbing 모델에는 없는 `F_fire` 항을 추가하여 화재 상황 특화
- 패닉 상태(Panic Factor)에 따른 `v₀` 동적 조절
- 취약계층의 `m`, `v₀`, `τ` 파라미터 차별화

### 2.2 KDE (Kernel Density Estimation) — 실시간 밀집도 분석

각 구역의 군중 밀집도를 연속적 확률 밀도 함수로 추정합니다.

**밀집도 추정 공식:**
```
f̂(x) = (1 / nh) × Σ K((x - xᵢ) / h)
```

- `n`: 해당 구역 내 보행자 수
- `h`: 대역폭 (bandwidth, 구역 크기에 따라 적응적 조절)
- `K`: 가우시안 커널 함수
- `xᵢ`: 각 보행자의 위치 좌표

**출력: 실시간 열지도 (Heatmap)**
| 밀집도 등급 | 인원/m² | 색상 | AI 대응 |
|------------|--------|------|---------|
| 여유 | < 1.0 | 녹색 | 정상 통행 |
| 보통 | 1.0~2.0 | 황색 | 모니터링 강화 |
| 혼잡 | 2.0~4.0 | 주황 | 우회 경로 활성화 |
| 위험 | 4.0~6.0 | 적색 | 해당 경로 차단, 강제 분산 |
| 압사 위험 | > 6.0 | 자주색 | 긴급 분산, 소방관 투입 |

**FireNavi 독자 적용:**
- 2D 평면 밀집도뿐 아니라 3D 층간 밀집도 동시 분석
- 시간 미분을 통한 밀집도 **변화율** 추적 (급격한 증가 = 병목 조기 경고)
- WiFi 위치추적 데이터와 직접 연동하여 CCTV 없이도 밀집도 산출

### 2.3 Greenshields 모델 (밀집도-속도 관계)

군중 밀집도가 증가하면 보행 속도가 비선형적으로 감소하는 관계를 정량화합니다.

```
v(ρ) = v_free × (1 - ρ / ρ_jam)
```

- `v(ρ)`: 밀집도 ρ에서의 실제 보행 속도
- `v_free`: 자유 보행 속도 (밀집도 0일 때의 속도)
- `ρ`: 현재 밀집도 (인원/m²)
- `ρ_jam`: 정체 밀집도 (보행 불가 임계점, ~6.0인/m²)

**임계점 분석:**
```
특정 복도에 90명 이상 → 이동 속도 급격 감소 → 병목 판정
→ Decision AI에 병목 정보 전달 → 우회 경로 생성
```

**FireNavi 독자 적용:**
- 복도 폭, 계단 경사도, 문 크기에 따른 `ρ_jam` 동적 조절
- 취약계층 혼재 비율에 따른 실효 밀집도 보정
- 양방향 통행(대피자 vs 소방관) 시의 실효 폭 감소 반영

### 2.4 Multi-Agent Simulation (개별 에이전트 행동 모델링)

각 대피자를 독립적인 에이전트로 모델링하여 개별 행동을 시뮬레이션합니다.

**에이전트 속성:**
```javascript
Agent {
  id: unique_id,
  type: "healthy" | "elderly" | "child" | "wheelchair" | "pregnant" | "injured",
  position: {x, y, deck},
  velocity: {vx, vy},
  health_profile: {
    max_speed: type_dependent,
    stair_capable: boolean,
    smoke_sensitivity: multiplier,
    panic_level: 0.0 ~ 1.0
  },
  assigned_exit: exit_id,
  current_path: [waypoints],
  companion: agent_id | null  // 동반 보호자
}
```

---

## 3. 대피자 유형 분류 체계

### 6가지 유형별 이동 특성

| 유형 | 코드 | 자유 속도 | 계단 속도 | 계단 이용 | 연기 민감도 | 경로 색상 |
|------|------|----------|----------|----------|-----------|---------|
| 건강한 성인 | H | 1.4 m/s | 0.8 m/s | 가능 | 1.0x | #4A90D9 (파랑) |
| 고령자 | E | 0.7 m/s | 0.4 m/s | 가능(저속) | 1.5x | #F59E0B (주황) |
| 어린이 | C | 1.0 m/s | 0.5 m/s | 가능 | 2.0x | #8B5CF6 (보라) |
| 휠체어 사용자 | W | 0.9 m/s | 불가 | **불가** | 1.5x | #EC4899 (핑크) |
| 임산부 | P | 0.8 m/s | 0.5 m/s | 가능(저속) | 1.8x | #14B8A6 (청록) |
| 부상자 | I | 0.5 m/s | 0.3 m/s | 제한적 | 2.0x | #EF4444 (빨강) |

### 동반 보호자 동기화

```
취약자 A ←→ 보호자 B
  ↓
두 사람의 경로를 동일하게 유지
  ↓
이동 속도는 느린 쪽(취약자)에 맞춤
  ↓
분리 거리 > 5m 시 경고 + 경로 재계산
```

---

## 4. 데이터 흐름

```
[WiFi 위치추적]      [IoT 센서]        [ID 프로필 카드]
(2~3m 정밀도)       (인원 카운트)      (건강 정보)
      ↓                  ↓                   ↓
┌─────────────────────────────────────────────────┐
│            Behavior AI Engine                   │
│                                                 │
│  ┌──────────┐   ┌───────────┐                  │
│  │ KDE      │   │ 유형 분류  │                  │
│  │ 밀집도 분석│   │ 6가지 유형 │                  │
│  └────┬─────┘   └─────┬─────┘                  │
│       ↓                ↓                        │
│  ┌─────────────────────────────┐               │
│  │ Social Force Model          │               │
│  │ (F_desired + F_social +     │               │
│  │  F_wall + F_fire)           │               │
│  └────────────┬────────────────┘               │
│               ↓                                 │
│  ┌─────────────────────────────┐               │
│  │ Greenshields                │               │
│  │ (밀집도 → 속도 변환)         │               │
│  └────────────┬────────────────┘               │
│               ↓                                 │
│  [출력: 밀집도 맵 + 병목 경고 + 유형별 속도]      │
└───────────────┬─────────────────────────────────┘
                ↓
      ┌─────────┴─────────┐
      ↓                    ↓
[Decision AI]        [Dynamic Risk Map]
(군중 분산 경로)      (밀집도 가중치 D)
```

---

## 5. 성능 사양

| 항목 | 사양 |
|------|------|
| 동시 처리 에이전트 | 6,000+ (크루즈선 기준) |
| 밀집도 갱신 주기 | 매 프레임 (60+ Hz) |
| 병목 탐지 응답 시간 | < 0.5초 |
| 위치 입력 정밀도 | 2~3m (WiFi 기반) |
| 유형 분류 정확도 | 100% (사전 등록 기반) |
| 패닉 감지 응답 | < 1초 |
| 밀집도 임계 경고 | 구역당 90명 초과 시 |

---

## 6. 차별성

### 기존 시스템 대비

| 비교 항목 | 기존 시스템 | FireNavi Behavior AI |
|----------|-----------|---------------------|
| 군중 분석 | CCTV 영상 사후 분석 | **실시간 KDE 열지도 + WiFi 위치추적** |
| 개인 구분 | 불가 (군중을 하나의 덩어리로 인식) | **6가지 유형 개별 에이전트 모델링** |
| 병목 예측 | 사후 감지 | **Greenshields 모델로 사전 예측** |
| 화재 연동 | 없음 | **F_fire 항 추가로 화재 회피 행동 반영** |
| 보호자 연동 | 없음 | **동반 보호자 경로 동기화** |
| 취약계층 | 구분 없음 | **연기 민감도 2배 안전 마진** |

### 학술 연구 대비

- 기존 Social Force Model은 **일반 보행자만** 고려 → FireNavi는 **6가지 유형 차별화**
- 기존 KDE 분석은 **2D 평면만** 처리 → FireNavi는 **3D 층간 밀집도 동시 분석**
- 화재 회피력(`F_fire`)은 **FireNavi 독자 개발** 항목
- 동반 보호자 경로 동기화는 **기존 연구에 없는 기능**

---

## 7. 다른 엔진과의 연동

| 연동 대상 | 제공 데이터 | 목적 |
|----------|-----------|------|
| → Decision AI | 구역별 밀집도, 병목 위치, 유형별 속도 | 경로 계산 시 혼잡 회피 |
| → Decision AI | CrowdPenalty = N × 40 | 출구 배정 시 혼잡 가중치 |
| → Dynamic Risk Map | 밀집도 가중치 D (w₃ = 0.20) | 위험지도의 군중 요소 |
| ← Prediction AI | 연기 확산 예측 경계 | F_fire 계산용 화재 위치 |
| ← WiFi 위치추적 | 전체 승객 실시간 위치 | KDE 입력 데이터 |
| ← ID 프로필 카드 | 건강 상태, 유형 분류 | 에이전트 속성 설정 |

---

*Behavior AI는 FireNavi의 "감각"입니다. 수천 명의 개별 인간을 이해하고, 그들이 만들어내는 군중의 흐름을 읽어내어, Decision AI가 충돌 없는 경로를 설계할 수 있도록 합니다.*
