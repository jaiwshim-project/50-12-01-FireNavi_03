# Prediction AI Engine — 미래를 보는 AI

> 실시간 화재 확산 예측 엔진  
> FireNavi 자체 개발 | 엔진 컬러: #EF4444 (Red/Blue)

---

## 1. 엔진 개요

Prediction AI는 화재 발생 시점부터 연기와 불꽃이 어디로, 얼마나 빠르게 확산될지를 **미래 시점까지 예측**하는 엔진입니다. 화재에서 가장 치명적인 것은 불꽃이 아니라 연기이며, 사망자의 70% 이상이 연기 흡입으로 사망합니다. Prediction AI는 연기가 대피자에게 도달하기 **전에** 위험 구역을 사전 차단하여 생존율을 극대화합니다.

### 핵심 역할
- 화재 확산 방향과 속도의 실시간 예측
- 연기 농도 분포의 시간별 예측 (t+10초, t+30초, t+60초)
- 가시거리 감소량 계산
- 온도 분포 및 유독가스 농도 예측
- Decision AI에 미래 위험 데이터 공급

---

## 2. 알고리즘 구조

### 2.1 Navier-Stokes 방정식 (CFD 기반 물리 시뮬레이션)

연기의 물리적 확산을 전산유체역학(CFD)으로 시뮬레이션합니다.

**연속 방정식 (질량 보존):**
```
∂ρ/∂t + ∇·(ρv) = 0
```

**운동량 방정식:**
```
ρ(∂v/∂t + v·∇v) = -∇p + μ∇²v + ρg + F_buoyancy
```

**에너지 방정식:**
```
ρcₚ(∂T/∂t + v·∇T) = k∇²T + Q_fire
```

- `ρ`: 기체 밀도
- `v`: 속도 벡터
- `p`: 압력
- `μ`: 동점성 계수
- `g`: 중력 가속도
- `F_buoyancy`: 부력 (온도차에 의한 상승 기류)
- `Q_fire`: 화재 열 방출률 (HRR, Heat Release Rate)

**FireNavi 독자 적용:**
- 선박/건물의 3D BIM 데이터와 연동하여 복도, 계단통, 환기구의 기하학적 구조를 반영
- 문 개폐 상태, HVAC(공조 시스템) 작동 여부에 따른 기류 변화를 실시간 업데이트
- 단순 CFD가 아닌 다중 화원(Multi-source fire) 시나리오 지원

### 2.2 Advection-Diffusion 모델 (연기 이동 예측)

연기가 기류를 타고 이동(이류)하면서 동시에 확산(확산)되는 과정을 모델링합니다.

```
∂C/∂t + v·∇C = D∇²C + S - λC
```

- `C`: 연기 농도 (mg/m³)
- `v`: 기류 속도 벡터 (Navier-Stokes에서 산출)
- `D`: 확산 계수
- `S`: 연기 발생원 (화원 위치에서의 연기 생성률)
- `λ`: 침강 계수 (중력에 의한 연기 입자 침강)

**FireNavi 독자 적용:**
- 복도와 계단통에서의 굴뚝 효과(Stack Effect)를 반영한 수직 연기 이동 예측
- 방화문·방화셔터 차단 시 연기 확산 경로 즉시 재계산
- 다층 구조물의 층간 연기 이동 (엘리베이터 샤프트, 계단실)

### 2.3 Beer-Lambert 법칙 (가시거리 계산)

연기 농도에 따른 가시거리 감소를 정량적으로 계산합니다.

```
V = K / (C × σ)
```

- `V`: 가시거리 (m)
- `K`: 비례 상수 (발광형 표지판: 8, 반사형: 3)
- `C`: 연기 농도 (mg/m³)
- `σ`: 연기 입자의 질량 광학 밀도 (m²/g)

**임계값 기준:**
| 가시거리 | 위험 등급 | AI 대응 |
|---------|----------|--------|
| > 10m | 안전 (Green) | 일반 경로 유지 |
| 5~10m | 주의 (Yellow) | 해당 경로 가중치 증가 |
| 2~5m | 위험 (Orange) | 경로 차단, 우회 경로 생성 |
| < 2m | 극도 위험 (Red) | 해당 구역 완전 차단 |

**취약계층 적용:**
- 일반 승객: 가시거리 기준 그대로 적용
- 취약계층: 안전 마진 2배 적용 (`SmokeCost_vulnerable = 2.0 × SmokeCost_normal`)

### 2.4 LSTM 딥러닝 네트워크 (학습 기반 예측 보정)

물리 시뮬레이션만으로는 실시간성을 확보하기 어렵기 때문에, LSTM(Long Short-Term Memory) 네트워크로 예측 속도와 정확도를 동시에 향상시킵니다.

**구조:**
```
Input Layer → LSTM Layer (128 units) → LSTM Layer (64 units) → Dense Layer → Output
```

**입력 데이터:**
- IoT 센서 실시간 데이터 (온도, CO 농도, 연기 감지기)
- CCTV 영상 기반 화원 크기 추정
- HVAC 시스템 상태
- 문/창문 개폐 상태
- 이전 시간 단계의 연기 분포

**출력 데이터:**
- t+10초, t+30초, t+60초 시점의 연기 농도 분포 맵
- 각 구역별 가시거리 예측값
- 온도 분포 예측
- 위험 등급 전이 시점 예측

**FireNavi 독자 적용:**
- CFD 물리 모델이 제공하는 정밀한 시뮬레이션 결과를 학습 데이터로 활용
- 실제 화재 발생 시에는 LSTM이 실시간(sub-second) 예측을 수행하고, CFD가 백그라운드에서 보정
- Transfer Learning을 통해 새로운 건물/선박 유형에 빠르게 적응

---

## 3. 데이터 흐름

```
[IoT 센서]          [CCTV]           [BIM 3D 모델]
    ↓                  ↓                   ↓
┌─────────────────────────────────────────────┐
│         Prediction AI Engine                │
│                                             │
│  ┌──────────┐    ┌──────────────┐          │
│  │ LSTM     │←──→│ Navier-Stokes│          │
│  │ (실시간) │    │ (정밀 보정)   │          │
│  └────┬─────┘    └──────────────┘          │
│       ↓                                     │
│  ┌──────────────────┐                      │
│  │ Advection-Diffusion                     │
│  │ (연기 이동 예측)   │                      │
│  └────┬─────────────┘                      │
│       ↓                                     │
│  ┌──────────────────┐                      │
│  │ Beer-Lambert      │                      │
│  │ (가시거리 계산)    │                      │
│  └────┬─────────────┘                      │
│       ↓                                     │
│  [출력: 시간별 위험 예측 맵]                  │
└─────────────┬───────────────────────────────┘
              ↓
     ┌────────┴────────┐
     ↓                  ↓
[Decision AI]     [Dynamic Risk Map]
(경로 결정)        (위험지도 갱신)
```

---

## 4. 성능 사양

| 항목 | 사양 |
|------|------|
| 예측 응답 시간 | < 1초 (LSTM 실시간 예측) |
| CFD 보정 주기 | 5~10초 (백그라운드) |
| 예측 시간 범위 | t+10초, t+30초, t+60초 |
| 공간 해상도 | 1m × 1m 그리드 |
| 지원 화원 수 | 다중 화원 동시 처리 |
| 연기 예측 정확도 | 90%+ (LSTM+CFD 결합) |
| 센서 입력 주기 | 1~2초 (IoT), 30fps (CCTV) |

---

## 5. 차별성

### 기존 시스템 대비

| 비교 항목 | 기존 시스템 | FireNavi Prediction AI |
|----------|-----------|----------------------|
| 예측 방식 | 사후 감지 (연기 감지기 알람) | **사전 예측** (연기 도달 전 경로 차단) |
| 정확도 | 단순 감지 (유/무) | **농도, 가시거리, 온도 정량 예측** |
| 시간 범위 | 현재 상태만 파악 | **60초 후 미래까지 예측** |
| 공간 범위 | 감지기 설치 지점만 | **전체 공간의 연속 분포** |
| 동적 반영 | 고정 모델 | **문 개폐, HVAC, 방화문 상태 실시간 반영** |
| AI 결합 | 없음 | **물리 모델 + 딥러닝 하이브리드** |

### 학술 연구 대비

- 대부분의 연구는 CFD **또는** 딥러닝 중 하나만 사용
- FireNavi는 **CFD(정밀도) + LSTM(실시간성)**을 동시에 결합한 하이브리드 아키텍처
- 단일 화원이 아닌 **다중 화원 동시 시나리오** 처리
- 건물 BIM 데이터와 실시간 연동하는 **동적 기하학 반영**

---

## 6. 다른 엔진과의 연동

| 연동 대상 | 제공 데이터 | 목적 |
|----------|-----------|------|
| → Decision AI | 시간별 위험 예측 맵, 구역별 위험 등급 | 경로 계산 시 미래 위험 회피 |
| → Behavior AI | 연기 확산 예측 경계 | 군중 밀집 구역과 연기 도달 구역 교차 분석 |
| → Dynamic Risk Map | 화재 열(F), 연기 밀도(S) 가중치 | R(x,y,t) = w₁F + w₂S + w₃D + w₄C |
| ← IoT 센서 | 실시간 온도, CO, 연기 농도 | LSTM 입력 데이터 |
| ← BIM 3D 모델 | 건물 구조, 문/창문 상태 | CFD 시뮬레이션 경계 조건 |

---

*Prediction AI는 FireNavi의 "눈"입니다. 아직 일어나지 않은 미래의 위험을 보고, 다른 두 엔진이 그 미래를 회피할 수 있도록 정보를 제공합니다.*
