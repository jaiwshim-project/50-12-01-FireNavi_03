# Decision AI Engine — 결정을 내리는 AI

> 취약 계층 최적 대피 경로 결정 엔진  
> FireNavi 자체 개발 | 엔진 컬러: #10B981 (Green)

---

## 1. 엔진 개요

Decision AI는 Prediction AI의 미래 위험 예측과 Behavior AI의 군중 분석 결과를 종합하여, **각 개인에게 매 순간 최적의 대피 경로를 결정**하는 최종 의사결정 엔진입니다. 단순히 최단 거리를 찾는 것이 아니라, 화재 위험 회피, 군중 충돌 방지, 출구별 부하 균형, 개인 건강 반영을 동시에 최적화합니다. 이것이 FireNavi가 "경로를 안내하는 것이 아니라 생존을 설계한다"고 말하는 이유입니다.

### 핵심 역할
- 6,000명 개인별 최적 대피 경로 동시 계산
- Dynamic Risk Map 기반 위험 회피 경로 생성
- 출구별 인원 균등 분산 (Exit Crowd Balancing)
- 취약계층 우선 경로 배정 (α=2.0 안전 마진)
- 소방관 최적 진입 경로 및 배치 위치 산출
- 0.1초 이내 전체 경로 재계산

---

## 2. 알고리즘 구조

### 2.1 Dynamic Risk Map (동적 위험지도 합성)

Decision AI의 모든 경로 결정의 기반이 되는 실시간 위험지도입니다.

**위험도 합성 공식:**
```
R(x, y, t) = w₁F(x,y,t) + w₂S(x,y,t) + w₃D(x,y,t) + w₄C(x,y,t)
```

| 요소 | 기호 | 가중치 | 데이터 출처 |
|------|------|--------|-----------|
| 화재 열 (온도) | F | w₁ = 0.35 | Prediction AI |
| 연기 밀도 (가시거리) | S | w₂ = 0.30 | Prediction AI |
| 군중 밀집도 | D | w₃ = 0.20 | Behavior AI |
| 구조물 붕괴 위험 | C | w₄ = 0.15 | BIM 내화등급 데이터 |

**위험도 범위:** 0.0 (완전 안전) ~ 1.0 (극도 위험)

**갱신 주기:** 매 초 (Prediction AI + Behavior AI 출력과 동기)

**FireNavi 독자 설계:**
- 4가지 이질적 위험 요소를 **단일 연속 스칼라 필드**로 합성
- 시간 차원 포함 — 현재 위험도뿐 아니라 **미래 위험도 반영**
- 가중치 `w₁~w₄`는 화재 진행 단계에 따라 동적 조절
  - 초기: 화재 열 비중 증가 (w₁ ↑)
  - 중기: 연기 확산 비중 증가 (w₂ ↑)
  - 후기: 구조물 붕괴 비중 증가 (w₄ ↑)

### 2.2 Safety-First A* 알고리즘 (대피자용 경로 탐색)

표준 A* 알고리즘에 위험지도 페널티를 추가한 FireNavi 독자 변형입니다.

**비용 함수:**
```
f(n) = g(n) + h(n) + R(n)
```

- `g(n)`: 시작 노드에서 현재 노드까지의 누적 비용
- `h(n)`: 현재 노드에서 목표(출구)까지의 휴리스틱 거리
- `R(n)`: Dynamic Risk Map에서의 위험 페널티

**위험 페널티 상세:**
```
R(n) = FirePenalty(n) × α + SmokePenalty(n) × α + CrowdPenalty(n)
```

- `FirePenalty(n)`: 해당 노드의 화재 위험도 (Prediction AI 제공)
- `SmokePenalty(n)`: 연기 농도에 따른 가시거리 페널티
- `CrowdPenalty(n)`: N × 40 (해당 노드 경유 예정 인원 × 40)
- `α`: 취약계층 안전 마진 계수
  - 일반 승객: α = 1.0
  - 취약계층: α = 2.0

**FireNavi 독자 설계:**
- 표준 A*는 정적 그래프 → FireNavi는 **매 초마다 가중치가 변하는 동적 그래프**
- `R(n)`에 **미래 시점의 위험도** 반영 (Prediction AI가 t+30초 예측 제공)
- CrowdPenalty로 **다른 대피자의 경로까지 고려**하여 충돌 사전 방지

### 2.3 Straight-Line A* 알고리즘 (소방관용 경로 탐색)

소방관은 화재를 향해 진입해야 하므로, 대피자와는 정반대의 경로 로직을 적용합니다.

```
f_ff(n) = g(n) + h(n)
// 위험 페널티 R(n)을 적용하지 않음
// 순수 거리 기반 최단 경로
```

**소방관 배치 위치 점수:**
```
PositionScore(P) = Upwind(P) + ExitAccess(P) - PassengerBlock(P)
                   + MultiFireCover(P) - FFOverlap(P) + CorridorBonus(P)
```

| 구성 요소 | 최대 점수 | 산출 방법 |
|----------|----------|----------|
| Upwind (풍상 접근) | 40점 | 바람 방향 정렬도 (±30°/±60°/±90°) |
| ExitAccess (출구 접근성) | 30점 | 가장 가까운 출구까지 거리 |
| PassengerBlock (대피자 간섭) | -5N점 | 대피 경로와의 교차 인원 수 |
| MultiFireCover (다중 화원 커버) | 10점 | 50m 이내 동시 커버 가능 화원 수 |
| FFOverlap (소방관 중복) | -20점 | 40m 이내 다른 소방관 존재 시 |
| CorridorBonus (복도 보너스) | 15점 | 개방된 복도 선호 |

**화원별 위협 점수 (소방관 투입 우선순위):**
```
ThreatScore(F) = PassengerCount(F) × 100
                + BlockedPassengers(F) × 30
                + SmokeAffectedPassengers(F) × 15
                + ExitThreats(F) × 40
                + FireRadius(F) × 2
                - ExistingFirefighters(F) × 80
```

### 2.4 Exit Crowd Balancing (출구 부하 균형 배정)

모든 대피자가 가장 가까운 출구로만 몰리면 특정 출구에 극심한 혼잡이 발생합니다. Exit Crowd Balancing은 전체 대피 시간을 최소화하도록 출구별 인원을 최적 분배합니다.

**출구 배정 점수:**
```
Score(exit_j, person_i) = Distance(i, j)
                          + FirePenalty(path_ij) × α
                          + SmokePenalty(path_ij) × α
                          + CrowdPenalty(exit_j)
```

```
CrowdPenalty(exit_j) = N_assigned(exit_j) × 40
```

**분배 로직:**
1. 모든 대피자에 대해 모든 출구까지의 Score 계산
2. Score가 가장 낮은 출구를 배정
3. 배정할 때마다 해당 출구의 `N_assigned` 증가 → CrowdPenalty 자동 증가
4. 결과적으로 먼 출구라도 혼잡하지 않으면 배정될 수 있음

**FireNavi 독자 설계:**
- 단순 최근접 출구 배정이 아닌 **전역 최적화**
- 취약계층은 α=2.0 적용으로 **화재/연기 회피 우선**
- 출구별 **통과 용량(throughput)** 반영 (문 폭, 계단 폭에 따른 차등)

### 2.5 Health-Aware Routing (개인 건강 반영 경로)

ID 프로필 카드에 등록된 건강 정보를 기반으로 경로를 차별화합니다.

**유형별 경로 제약:**

| 유형 | 계단 | 엘리베이터 | 경사로 | 연기 구역 | 경로 특성 |
|------|------|-----------|--------|----------|---------|
| 건강한 성인 | 사용 | 비사용 | 사용 | 허용(제한적) | 최단 거리 우선 |
| 고령자 | 저속 사용 | **우선 사용** | **우선** | 2배 회피 | 완만한 경로 |
| 어린이 | 사용 | 사용 | 사용 | 2배 회피 | 단순 경로, 짧은 거리 |
| 휠체어 | **불가** | **필수** | **필수** | 2배 회피 | 배리어프리 경로만 |
| 임산부 | 저속 사용 | 우선 사용 | 우선 | 1.8배 회피 | 최소 노력 경로 |
| 부상자 | 제한적 | 우선 사용 | 우선 | 2배 회피 | 의료 접근점 경유 |

**경로 차별화 시각화:**

| 유형 | 색상 | 선 패턴 | 선 굵기 |
|------|------|---------|--------|
| 건강한 성인 | #4A90D9 | 실선 | 1.5px |
| 고령자 | #F59E0B | [6,3] 점선 | 2.0px |
| 어린이 | #8B5CF6 | [4,2] 점선 | 2.0px |
| 휠체어 | #EC4899 | [8,4] 점선 | 2.5px |
| 임산부 | #14B8A6 | [5,3] 점선 | 2.0px |
| 부상자 | #EF4444 | [3,3] 점선 | 2.5px |

### 2.6 CBS (Conflict-Based Search) — 다중 경로 충돌 해소

6,000명의 경로가 교차점에서 충돌하지 않도록 상위 레벨에서 조율합니다.

```
1단계: 각 에이전트에 대해 독립적으로 A* 최적 경로 산출
2단계: 교차점에서의 시간적·공간적 충돌 탐지
3단계: 충돌 발생 시 관련 에이전트 중 하나에 시간/공간 제약 추가
4단계: 제약 조건 하에서 해당 에이전트 경로 재계산
5단계: 모든 충돌이 해소될 때까지 반복
```

### 2.7 PPO 강화학습 (시뮬레이션 기반 정책 최적화)

수만 번의 시뮬레이션을 통해 최적 정책을 학습합니다.

**학습 환경:**
- 상태(State): 화재 위치, 연기 분포, 군중 분포, 대피자 위치
- 행동(Action): 각 대피자의 다음 이동 방향/출구 변경
- 보상(Reward): 전체 대피 완료 시간의 역수 + 사상자 0명 보너스

**학습 성과:**
- 시뮬레이션 대비 실제 상황에서 100ms 이내 최적 경로 산출
- 학습된 정책은 미경험 시나리오에도 일반화

---

## 3. 데이터 흐름

```
[Prediction AI]              [Behavior AI]
 ↓ 미래 위험 예측               ↓ 밀집도 + 유형별 속도
 ↓ F(x,y,t), S(x,y,t)        ↓ D(x,y,t), 병목 위치
      ↓                            ↓
┌──────────────────────────────────────────────────┐
│              Decision AI Engine                  │
│                                                  │
│  ┌────────────────────────────────┐              │
│  │ Dynamic Risk Map 합성          │              │
│  │ R = w₁F + w₂S + w₃D + w₄C    │              │
│  └──────────┬─────────────────────┘              │
│             ↓                                    │
│  ┌────────────────────────────────┐              │
│  │ Health-Aware 유형 분류          │              │
│  │ (α = 1.0 일반 / 2.0 취약계층)   │              │
│  └──────────┬─────────────────────┘              │
│             ↓                                    │
│  ┌──────────┴──────────┐                        │
│  ↓                      ↓                        │
│ [대피자 경로]          [소방관 경로]               │
│ Safety-First A*       Straight-Line A*           │
│ f = g + h + R         f = g + h                  │
│       ↓                      ↓                   │
│ Exit Crowd Balancing   Position Scoring           │
│       ↓                      ↓                   │
│ CBS 충돌 해소          Threat Scoring              │
│       ↓                      ↓                   │
│ [6,000명 개별 경로]   [소방관 배치 + 진입 경로]     │
└──────────────────────────────────────────────────┘
              ↓
      [실시간 경로 시각화]
      [모바일 앱 / HMD 전송]
```

---

## 4. 성능 사양

| 항목 | 사양 |
|------|------|
| 경로 계산 시간 | < 100ms (6,000명 동시) |
| 경로 갱신 주기 | 매 프레임 (60+ Hz) |
| 동시 처리 인원 | 6,000+ (크루즈선 기준) |
| 출구 분산 정확도 | 최적 균형 대비 95%+ |
| 충돌 회피율 | 100% (CBS 보장) |
| 메모리 최적화 | MinHeap + Spatial Grid |
| 소방관 동시 배치 | 최대 50명 |
| 다중 화원 처리 | 동시 5개+ |

---

## 5. 차별성

### 기존 시스템 대비

| 비교 항목 | 기존 시스템 | FireNavi Decision AI |
|----------|-----------|---------------------|
| 경로 방식 | 고정 경로 (비상구 안내판) | **개인별 실시간 동적 경로** |
| 위험 반영 | 없음 | **4요소 가중합 위험지도 반영** |
| 개인 차별화 | 전원 동일 경로 | **6가지 유형별 맞춤 경로** |
| 출구 분산 | 최근접 출구만 | **전역 최적 출구 배정** |
| 충돌 방지 | 없음 | **CBS 다중 에이전트 충돌 해소** |
| 소방관 지원 | 없음 | **최적 진입 경로 + 배치 위치 산출** |
| 미래 반영 | 현재 상태만 | **t+60초까지 미래 위험 반영** |
| 응답 속도 | 해당 없음 | **0.1초 이내 전체 재계산** |

### 학술 연구 대비

- 대부분의 연구는 A* 또는 강화학습 중 하나만 사용 → FireNavi는 **A* + CBS + RL 삼중 결합**
- 기존 연구의 출구 배정은 정적 → FireNavi는 **실시간 동적 출구 재배정**
- 대피자와 소방관을 동시에 처리하는 시스템은 **FireNavi가 유일**
- 취약계층 α=2.0 안전 마진은 **FireNavi 독자 설계**
- 위험지도의 **시간 차원 포함 (미래 위험)**은 기존 연구에서 드문 접근

---

## 6. 다른 엔진과의 연동

| 연동 대상 | 수신 데이터 | 활용 목적 |
|----------|-----------|----------|
| ← Prediction AI | F(x,y,t): 화재 열 분포 | Risk Map의 w₁F 요소 |
| ← Prediction AI | S(x,y,t): 연기 밀도 분포 | Risk Map의 w₂S 요소 + 가시거리 페널티 |
| ← Behavior AI | D(x,y,t): 군중 밀집도 | Risk Map의 w₃D 요소 + CrowdPenalty |
| ← Behavior AI | 유형별 이동 속도 | Health-Aware 경로 계산 |
| ← Behavior AI | 병목 위치 | 해당 구간 우회 경로 생성 |
| ← BIM 3D 모델 | C(x,y): 구조물 내화등급 | Risk Map의 w₄C 요소 |
| ← WiFi 위치추적 | 전체 인원 실시간 위치 | 경로 시작점 + 경로 진행 모니터링 |
| → 모바일 앱 / HMD | 개인별 대피 경로 | 대피자 안내, 소방관 전술 지원 |
| → Monte Carlo 시뮬레이션 | 전체 경로 데이터 | 대피 시나리오 검증 |

---

## 7. 대피 완료까지의 전체 프로세스

```
[화재 발생]
    ↓ 0.0초
[Prediction AI 활성화] — 연기 확산 예측 시작
    ↓ 0.1초
[Behavior AI 활성화] — 전체 인원 위치 파악 + 유형 분류
    ↓ 0.2초
[Decision AI 활성화]
    ↓
  ┌─ Dynamic Risk Map 생성 (0.05초)
  ├─ 출구 배정 최적화 (0.02초)
  ├─ 6,000명 개별 A* 경로 계산 (0.05초)
  ├─ CBS 충돌 해소 (0.03초)
  └─ 소방관 배치 위치 산출 (0.01초)
    ↓ 0.3초 (화재 발생 후)
[전체 대피 경로 확정 → 모바일 앱 전송]
    ↓ 매 1초마다
[3개 엔진 동기 갱신 → 경로 실시간 재계산]
    ↓
[전원 대피 완료] — 기존 18분 → 11분 (40% 단축)
```

---

*Decision AI는 FireNavi의 "두뇌"입니다. Prediction AI가 보여주는 미래와 Behavior AI가 읽어내는 군중을 종합하여, 6,000명 한 명 한 명의 생존을 매 순간 설계합니다. 모든 사람이 같은 경로로 대피하는 시대는 끝났습니다.*
