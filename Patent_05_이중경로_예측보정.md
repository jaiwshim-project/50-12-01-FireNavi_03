# 특허 명세서 — PAT-NEW-05

## 발명의 명칭

**화재 연기 확산 예측에서 LSTM 실시간 추론과 CFD 정밀 해석의 이중 경로 병행 실행 및 적응적 가중 병합에 의한 실시간성과 정밀도 동시 확보 방법**

---

## 기술분야

본 발명은 화재 연기 확산 예측 기술에 관한 것으로, 보다 구체적으로는 LSTM 딥러닝에 의한 밀리초 단위 실시간 추론(Fast Path)과 Navier-Stokes 방정식의 수치 해석에 의한 고정밀 시뮬레이션(Slow Path)을 동일 시스템 내에서 병행 실행하고, Slow Path 결과에 의한 Fast Path 온라인 보정과, 두 경로 출력의 적응적 가중 병합을 수행하여, 실시간성과 정밀도를 동시에 확보하는 방법에 관한 것이다.

---

## 배경기술

화재 연기 확산 예측에는 두 가지 접근법이 존재한다:

**접근법 1: CFD 기반 물리 시뮬레이션**
- Navier-Stokes 방정식의 수치 해석에 의해 물리적으로 정확한 예측이 가능하다.
- 그러나 3D 공간의 편미분 방정식 해석에 수 초~수 분이 소요되어 실시간 대피 안내에 사용할 수 없다.

**접근법 2: 딥러닝 기반 추론**
- 학습된 LSTM/CNN 모델에 의해 밀리초 단위 추론이 가능하다.
- 그러나 학습 데이터에 없는 시나리오(미학습 건물 구조, 특이한 화재 패턴)에서 정확도가 급격히 저하된다.

종래 기술의 한계:

1. 두 접근법은 각각 독립적으로 연구되어 왔으며, 동일 시스템 내에서 병행 실행하여 상호 보완하는 구체적 방법이 제시되지 않았다.
2. CFD 결과를 딥러닝 모델의 실시간 가중치 보정에 활용하는 온라인 학습 방법이 제시되지 않았다.
3. 두 경로의 출력을 병합할 때, 각 경로의 "신선도(freshness)"에 따라 가중치를 적응적으로 조절하는 방법이 제시되지 않았다.

---

## 해결하려는 과제

본 발명은 LSTM Fast Path의 실시간성과 CFD Slow Path의 정밀도를 동시에 확보하기 위해, (1) 이중 경로 병행 실행 아키텍처, (2) Slow Path에 의한 Fast Path 온라인 보정, (3) 경과 시간 기반 적응적 가중 병합을 제공하는 것을 목적으로 한다.

---

## 과제의 해결수단

### 1. 이중 경로 아키텍처

```
┌──────────────────────────────────────────────┐
│              Prediction Engine               │
│                                              │
│  [IoT 센서 데이터 X(t)]                       │
│       ↓              ↓                       │
│  ┌────────┐    ┌──────────┐                  │
│  │Fast Path│    │Slow Path │                  │
│  │ (LSTM)  │    │ (CFD)    │                  │
│  │ 15ms    │    │ 5~10초   │                  │
│  └───┬─────┘    └────┬─────┘                  │
│      ↓               ↓                       │
│      ↓          ┌────────────┐               │
│      ↓          │온라인 보정기│               │
│      ↓          │(Loss→∇w)  │               │
│      ↓          └────┬───────┘               │
│      ↓               ↓                       │
│  ┌─────────────────────────┐                 │
│  │ 적응적 가중 병합기        │                 │
│  │ P_out = β×P_fast        │                 │
│  │       + (1-β)×P_slow    │                 │
│  └───────────┬─────────────┘                 │
│              ↓                               │
│         [P_out + ts_pred]                    │
└──────────────────────────────────────────────┘
```

### 2. Fast Path 상세 (LSTM 실시간 추론)

#### 2.1 입력 텐서 구성

```
단계 F1: 센서 데이터 수집
    시점 t에서 n개 센서의 측정값을 수집한다:
    X(t) = [T₁(t),...,Tₙ(t),     # 온도 (℃)
            CO₁(t),...,COₙ(t),    # CO 농도 (ppm)
            S₁(t),...,Sₙ(t),     # 연기 감지 (0~1)
            H₁(t),...,Hₙ(t)]     # HVAC 상태 (0/1)
    벡터 길이: 4n

단계 F2: 시퀀스 구성
    최근 k개 시간 단계의 데이터를 시퀀스로 구성한다:
    Sequence = [X(t-k+1), X(t-k+2), ..., X(t)]
    텐서 형상: (1, k, 4n)
    여기서 k=10 (10초분의 이력 데이터)

단계 F3: 공간 매핑 보조 입력
    센서 위치를 그리드 좌표로 매핑한 공간 텐서를 구성한다:
    SpatialMap = Grid(W×H×D)
    여기서 W, H, D는 건물의 가로, 세로, 층수 그리드 크기
    그리드 해상도: 1m × 1m × 1층
```

#### 2.2 LSTM 네트워크 구조

```
Layer 1: LSTM(input_size=4n, hidden_size=128, num_layers=1)
         → Dropout(0.2)
Layer 2: LSTM(input_size=128, hidden_size=64, num_layers=1)
         → Dropout(0.2)
Layer 3: Dense(input_size=64, output_size=W×H×D×3)
         → Reshape(W, H, D, 3)

출력 3채널:
  Channel 0: 연기 농도 맵 C(x,y,z,t+Δt)  [mg/m³]
  Channel 1: 온도 분포 맵 T(x,y,z,t+Δt)  [℃]
  Channel 2: 가시거리 맵 V(x,y,z,t+Δt)   [m]
```

#### 2.3 다중 시점 예측

```
단계 F4: 세 시점의 예측을 순차 생성한다:
    P_fast_10 = LSTM(Sequence, Δt=10초)
    P_fast_30 = LSTM(Sequence, Δt=30초)
    P_fast_60 = LSTM(Sequence, Δt=60초)

    총 추론 시간: 15ms 이내 (GPU 가속 기준)
    CPU 전용: 25ms 이내

    P_fast = {P_fast_10, P_fast_30, P_fast_60}
    ts_fast = t (현재 시점의 센서 데이터 기반)
```

### 3. Slow Path 상세 (CFD 정밀 해석)

#### 3.1 Navier-Stokes 수치 해석

```
단계 S1: BIM 3D 모델로부터 계산 도메인을 구성한다.
    메쉬 해상도: 0.5m × 0.5m × 0.5m
    경계 조건: 벽면(no-slip), 문/창(유량 경계), 화원(열원)

단계 S2: 다음 연립 방정식을 수치적으로 해석한다:

    연속 방정식:
    ∂ρ/∂t + ∇·(ρv) = 0

    운동량 방정식:
    ρ(∂v/∂t + v·∇v) = -∇p + μ∇²v + ρg + F_b
    여기서 F_b = ρ₀gβ(T-T₀)ê_z (Boussinesq 근사 부력항)

    에너지 방정식:
    ρcₚ(∂T/∂t + v·∇T) = k∇²T + Q̇'''
    여기서 Q̇''' = HRR / V_fire (단위 체적당 열 방출률)

    연기 수송 방정식:
    ∂C/∂t + v·∇C = D_s∇²C + Ṡ - λ_d C
    여기서 D_s=0.1 m²/s, λ_d=0.01 s⁻¹

단계 S3: 해석 결과로 정밀 예측 맵을 생성한다:
    P_slow = {C_cfd(x,y,z,t+Δt), T_cfd(x,y,z,t+Δt), V_cfd(x,y,z,t+Δt)}
    ts_slow = t_start (해석 시작 시점의 센서 데이터 기반)

    소요 시간: 5~10초 (GPU 가속, 메쉬 크기 의존)
```

#### 3.2 동적 경계 조건 갱신

```
단계 S4: Slow Path 실행 중에도 경계 조건을 동적으로 갱신한다:

    매 1초마다:
    - 문 개폐 상태 변경 → 유량 경계 조건 갱신
    - HVAC 가동/정지 → 환기 경계 조건 갱신
    - 새로운 화원 감지 → 열원 추가

    경계 조건 변경 시 현재 시간 단계부터 재계산한다.
    (이전 시간 단계 결과는 유지)
```

### 4. 온라인 보정 메커니즘

Slow Path 결과로 Fast Path의 LSTM 가중치를 실시간 보정한다.

#### 4.1 보정 트리거

```
단계 O1: Slow Path가 P_slow 산출을 완료할 때마다
         온라인 보정이 트리거된다.
         빈도: 5~10초마다 1회
```

#### 4.2 보정 손실 함수

```
단계 O2: Fast Path가 동일 시점에 대해 산출한 예측과
         Slow Path의 정밀 결과를 비교한다.

    # P_slow의 입력 시점(ts_slow)에서 Fast Path가 산출했던 예측을 참조
    P_fast_at_ts_slow = cached_predictions[ts_slow]

    # 손실 계산
    Loss = (1/M) × Σ_m (P_fast_at_ts_slow[m] - P_slow[m])²
    여기서 M은 그리드 셀 수

    # 채널별 가중 손실
    Loss_total = λ_C × Loss_C + λ_T × Loss_T + λ_V × Loss_V
    여기서:
      λ_C = 0.5 (연기 농도 가중치 — 가장 중요)
      λ_T = 0.3 (온도 가중치)
      λ_V = 0.2 (가시거리 가중치)
```

#### 4.3 가중치 갱신

```
단계 O3: LSTM 가중치를 경사하강법으로 갱신한다.

    gradients = ∂Loss_total / ∂W_lstm
    W_lstm ← W_lstm - lr × gradients

    여기서:
      lr = 0.001 (학습률)
      갱신은 최종 Dense 레이어만 대상 (부분 미세 조정)
      LSTM 내부 가중치는 동결하여 안정성을 유지한다.

    소요 시간: 2ms 이내 (단일 역전파)
```

#### 4.4 보정 효과 검증

```
단계 O4: 보정 전후의 Loss를 비교한다.

    if Loss_after > Loss_before × 1.1:
        # 보정이 오히려 악화 → 롤백
        W_lstm ← W_lstm_backup
        lr ← lr × 0.5  # 학습률 감소
    else:
        # 보정 성공 → 유지
        W_lstm_backup ← W_lstm
```

### 5. 적응적 가중 병합

두 경로의 출력을 "신선도"에 따라 적응적으로 병합한다.

#### 5.1 병합 공식

```
P_out(t) = β(t) × P_fast(t) + (1 - β(t)) × P_slow(t_last)
```

#### 5.2 β의 적응적 산출

```
β(t) = min(1.0, elapsed_slow / T_slow)

여기서:
  elapsed_slow = t - ts_slow_last  (마지막 Slow Path 완료 이후 경과 시간)
  T_slow = Slow Path 실행 주기 (5~10초)

β의 작동:
  Slow Path 완료 직후 (elapsed=0): β=0 → P_out ≈ P_slow (정밀 우선)
  Slow Path 중간 (elapsed=T/2):   β=0.5 → 균등 병합
  Slow Path 직전 (elapsed≈T):     β≈1.0 → P_out ≈ P_fast (실시간 우선)
```

#### 5.3 이상 상황 처리

```
case 1: Slow Path 지연 (elapsed > 2 × T_slow)
    β = 1.0 (Fast Path만 사용)
    경고 플래그 설정: SLOW_PATH_DELAYED

case 2: Fast Path 이상 (P_fast에 NaN 또는 음수 포함)
    β = 0.0 (Slow Path만 사용)
    경고 플래그 설정: FAST_PATH_ERROR
    LSTM 네트워크 재초기화 트리거

case 3: 양쪽 모두 이상
    마지막 유효한 P_out을 사용하되,
    경과 시간에 따라 감쇄:
    P_fallback = P_last_valid × exp(-elapsed / 5.0)
```

### 6. 캐시 관리

```
Fast Path의 과거 예측을 캐시하여 온라인 보정에 활용한다.

PredictionCache = CircularBuffer(capacity=20)

매 Fast Path 실행 시:
    cache.push({
        timestamp: ts_fast,
        prediction: P_fast,
        sensor_input: X(t)
    })

Slow Path 완료 시:
    matching_entry = cache.find(ts_slow)
    if matching_entry:
        P_fast_at_ts_slow = matching_entry.prediction
        # 온라인 보정 실행 (단계 O2~O4)
    else:
        # 캐시 미스 → 보정 생략, 다음 사이클 대기
```

---

## 청구범위

**[청구항 1]**
화재 연기 확산을 예측하는 방법에 있어서,
(a) LSTM 딥러닝 네트워크에 IoT 센서 시퀀스 데이터를 입력하여, 미래 시점의 공간별 연기 농도, 온도, 가시거리 예측 맵을 15ms 이내에 산출하는 Fast Path를 실행하는 단계;
(b) 상기 Fast Path와 병행하여, 동일 센서 데이터를 초기 조건으로 Navier-Stokes 방정식의 수치 해석을 수행하여 정밀 예측 맵을 5~10초 주기로 산출하는 Slow Path를 실행하는 단계;
(c) Slow Path 산출 완료 시, Fast Path가 동일 시점에 대해 캐시에 저장해 둔 예측과 Slow Path 결과의 손실을 산출하고, 상기 손실에 기초하여 LSTM의 출력층 가중치를 경사하강법으로 갱신하는 온라인 보정 단계;
(d) Fast Path 출력과 Slow Path 출력을, Slow Path 완료 이후 경과 시간에 비례하는 가중치(β)로 병합하여 최종 예측 맵을 생성하는 적응적 병합 단계;
를 포함하는 것을 특징으로 하는 이중 경로 화재 연기 확산 예측 방법.

**[청구항 2]**
제1항에 있어서,
상기 적응적 병합 단계에서 가중치 β는 β = min(1.0, elapsed / T_slow)이고, Slow Path 완료 직후에는 β=0으로 Slow Path 결과가 우세하며, 경과 시간이 증가할수록 β→1.0으로 Fast Path 결과가 우세해지는 것을 특징으로 하는 방법.

**[청구항 3]**
제1항에 있어서,
상기 온라인 보정 단계에서 보정 후 손실이 보정 전 손실의 110%를 초과하면 가중치를 롤백하고 학습률을 50% 감소시키는 안전 장치를 포함하는 것을 특징으로 하는 방법.

**[청구항 4]**
제1항에 있어서,
상기 온라인 보정은 LSTM 네트워크의 최종 Dense 레이어 가중치만을 대상으로 하고, LSTM 내부 게이트 가중치는 동결하여 시계열 학습의 안정성을 유지하는 것을 특징으로 하는 방법.

**[청구항 5]**
제1항에 있어서,
상기 Slow Path는 실행 중에 문 개폐, HVAC 상태 변경, 새로운 화원 감지에 따라 경계 조건을 동적으로 갱신하고, 경계 조건 변경 시점부터 재계산하는 것을 특징으로 하는 방법.

**[청구항 6]**
제1항에 있어서,
상기 손실 함수는 연기 농도, 온도, 가시거리의 세 채널에 대해 각각 λ_C=0.5, λ_T=0.3, λ_V=0.2의 가중치를 적용한 가중 평균 제곱 오차인 것을 특징으로 하는 방법.

**[청구항 7]**
제1항 내지 제6항 중 어느 한 항의 방법을 실행하기 위한 프로그램이 기록된 컴퓨터 판독 가능 기록매체.

---

## 발명의 효과

1. Fast Path에 의해 15ms 이내의 실시간 예측이 보장되면서, Slow Path에 의해 물리적 정밀도가 유지되어, 실시간성과 정밀도의 트레이드오프를 해소한다.
2. 온라인 보정에 의해 LSTM 모델이 현재 화재의 고유 특성에 실시간으로 적응하여, 미학습 시나리오에서의 예측 정확도 저하를 최소화한다.
3. 적응적 가중 병합(β)에 의해 Slow Path의 "신선도"가 자동으로 반영되어, 별도의 유효성 판단 로직 없이 최적 병합이 달성된다.
4. 롤백 안전 장치에 의해 온라인 보정이 오히려 악화를 유발하는 경우 즉시 복구되어 시스템 안정성이 보장된다.
5. Dense 레이어만 보정하는 부분 미세 조정에 의해, 2ms 이내에 보정이 완료되어 파이프라인 사이클에 영향을 주지 않는다.

---

## 도면의 간단한 설명

- [도 1] 이중 경로 아키텍처 전체 구성도
- [도 2] LSTM 네트워크 구조 및 입출력 텐서 형상
- [도 3] Fast Path / Slow Path 타이밍 다이어그램
- [도 4] 온라인 보정 메커니즘 흐름도 (캐시 참조 포함)
- [도 5] β 적응적 가중치 변화 그래프
- [도 6] 롤백 안전 장치 동작 시나리오
- [도 7] 이상 상황별 폴백 로직 흐름도

---

*발명자: 심재우 | 출원인: FireNavi*
